apiVersion: v1
kind: Service
metadata:
  name: antigravity-api
  labels:
    app: antigravity
spec:
  type: ClusterIP
  selector:
    app: antigravity
    component: api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
